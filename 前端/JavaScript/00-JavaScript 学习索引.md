---
title: JavaScript 学习索引
tags:
  - 前端
  - JavaScript
aliases:
  - JS笔记
---

# JavaScript 学习索引

> [!source] 课程来源
> 已按 `html.pdf` 第 29–53 页补充。课程中的 `var`、jQuery 1.8.3 和部分正则属于旧式写法，笔记中同时给出适用范围和现代替代方案。

## 01 基础

- [[01-JavaScript 基础与输出]]：JavaScript 的定位、引入位置、输出方式、大小写、关键字和调试。
- [[02-变量、数据类型与类型转换]]：`var` / `let` / `const`、数据类型、`null`、`undefined`、`NaN` 和类型转换。

## 02 流程控制与函数

- [[01-运算符、分支与循环]]：比较运算符、`if`、`switch`、`for`、`for...in`、`for...of`、`while`、`break` 和 `continue`。
- [[02-函数]]：函数声明、函数表达式、箭头函数、参数、返回值、作用域和回调函数。

## 03 DOM 与事件

- [[01-DOM 节点与事件]]：节点选择、内容和样式修改、表单状态、常见事件和事件绑定方式。

## 04 常用对象

- [[01-数组、对象与 Date]]：数组、对象、日期时间的读取、设置和格式化。

## 05 交互练习

- [[01-数字时钟]]：`Date`、函数、`setInterval()` 和 `setTimeout()`。
- [[02-复选框全选与显示隐藏]]：复选框批量操作、类名切换和元素显示隐藏。

## 06 jQuery

- [[01-jQuery 表单验证]]：jQuery 选择器、事件、DOM 操作、课程正则和注册表单验证思路。

## 课程大作业备注

`html.pdf` 第 52–53 页主要是贵美商城分组、提交方式和答辩要求，不属于语言知识点，因此未收录为教程正文。